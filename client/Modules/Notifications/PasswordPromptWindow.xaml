<Window x:Class="AMadmin.UiAgent.PasswordPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AMadmin — пароль"
        Width="340" SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        Background="#F4F5F7">
    <Border Margin="16" Background="White" CornerRadius="10" BorderBrush="#D8DBE0" BorderThickness="1" Padding="20">
        <StackPanel>
            <TextBlock Text="Это действие защищено паролем" FontSize="15" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,0,0,12"/>
            <PasswordBox x:Name="PasswordBox" Height="32" Padding="6,4" FontSize="14"/>
            <TextBlock x:Name="ErrorText" Text="Неверный пароль, попробуйте ещё раз." Margin="0,8,0,0" FontSize="13"
                       Foreground="#B91C1C" TextWrapping="Wrap" Visibility="Collapsed"/>
            <Grid Margin="0,16,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="1" Content="Отмена" Width="90" Height="32" Margin="0,0,8,0" IsCancel="True" Click="Cancel_Click"/>
                <Button Grid.Column="2" Content="OK" Width="90" Height="32" IsDefault="True" Click="Ok_Click"/>
            </Grid>
        </StackPanel>
    </Border>
</Window>
