-- IP, с которого агент последний раз выходил на связь. Раньше нигде не сохранялся —
-- на кассе за NAT/прокси REMOTE_ADDR не всегда "полезный" внешний IP, но это то же
-- самое, что видит сам сервер, и для поиска "с какого хоста это вообще было" достаточно.
ALTER TABLE pcs ADD COLUMN last_ip TEXT;
