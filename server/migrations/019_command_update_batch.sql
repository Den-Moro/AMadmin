-- Группировка нескольких команд, отправленных одним действием со страницы «Обновления»
-- (например, сразу оба файла агента на одни и те же кассы) — каждая команда всё равно
-- отдельная строка (это не меняет ни исполнение, ни существующую модель), просто
-- помечена общим update_batch_id, чтобы в истории команд показывались одной группой.
ALTER TABLE commands ADD COLUMN update_batch_id TEXT;
